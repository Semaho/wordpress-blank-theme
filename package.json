{
    "name": "wp-theme-sebastien",
    "version": "1.0.0",
    "description": "Sébastien's Wordpress boilerplate",
    "main": "gulpfile.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "sass:watch": "sass --watch assets/scss:assets/dist/css --style=compressed --source-map",
        "sass:compile": "sass assets/scss:assets/dist/css --style=compressed --source-map"
    },
    "author": "Sébastien Vignol",
    "license": "ISC",
    "browserslist": ["defaults"],
    "devDependencies" : {
        "sass": "latest"
    }
}
